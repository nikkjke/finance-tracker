# Docker / PostgreSQL development defaults
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_me
POSTGRES_DB=financetracker
POSTGRES_PORT=5432

# Optional API connection string override (used by ASP.NET Core env vars)
ConnectionStrings__DefaultConnection=Host=localhost;Port=5432;Database=financetracker;Username=postgres;Password=
